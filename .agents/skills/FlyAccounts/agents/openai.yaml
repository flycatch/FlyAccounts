interface:
  display_name: "FlyAccounts"
  short_description: "Repo-specific patterns and workflows for FlyAccounts"
  default_prompt: "Use the FlyAccounts repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true